# 关卡设计 · 第一章草案

状态：讨论稿，尚未接入游戏。关卡类型等待确认；以下以固定棋盘、固定方块序列的解谜关为首选方向。现有自由模式继续保留。

## 设计目标

先让玩家学会数「完整方块」、识别边连接，再引入搭桥与整块重力，最后要求主动制造连锁。难度来自棋盘关系与落点选择，而不是先提高下落速度。

当前 HUD 中的 Level 只是按落地数量提升的速度等级，不是关卡编号。加入关卡后两者必须区分：关卡模式显示关卡目标与剩余方块，避免继续显示误导性的速度等级。

## 建议的第一章：连接

| 关卡 | 名称 | 新学习点 | 目标 | 限制与反馈 |
| --- | --- | --- | --- | --- |
| 1 | 第三个伙伴 | 数完整方块，而非格子 | 消除 3 个红色方块 | 仅 1 个待落方块；两个预置红色 O 已接触，但不会自行消除 |
| 2 | 拐角不是连接 | 角接触不计数；连通组可以拐弯 | 消除 3 个红色方块 | 仅 1 个红色 O；保留另一色支撑块，不要求清空棋盘 |
| 3 | 一座桥 | 一个长条可以连接两侧分离的同色组 | 清空全部红色方块 | 仅 1 个红色 I；不把横行填满作为过关条件 |
| 4 | 换个方向 | 预判旋转后的形状与落点 | 在 2 个待落方块内完成指定消除 | 布局待制作；不以强制点击旋转按钮判断过关 |
| 5 | 撤去支撑 | 消除支撑后，完整形状一起下落 | 完成 2 连锁 | 布局待制作；不能依赖零散格子掉落或方块穿过阻挡 |
| 6 | 连起来想 | 先布置，再触发 | 在 3 个待落方块内完成 2 连锁 | 综合关；避免第一步存在意外直接解法 |

前三关提供可落地的棋盘草案；后三关在确认方向后制作布局、搜索可达解，并检查捷径。此表不代表六关已完成可玩验证。

## 前三关的棋盘草案

棋盘仍为 10 列 × 18 行；坐标从左上角 `(0, 0)` 起。每条记录表示一个完整方块，以当前 `Piece.Create` 的默认朝向定义。红色为颜色 0，绿色为颜色 1；颜色名称还应配合现有图案标记呈现。

### 01 · 第三个伙伴

- 初始：红 O `(0, 16)`，红 O `(2, 16)`。
- 待落序列：红 O。
- 参考操作：移动至左边界 x=4，直接落下；落在 `(4, 16)`，三块消除。
- 目标：累计消除至少 3 个方块。
- 提示：已经接触的两个红色 O 虽然包含八个格子，也只算两个方块。

### 02 · 拐角不是连接

- 初始：红 O `(0, 16)`，绿 O `(2, 16)`，红 O `(2, 14)`。
- 待落序列：红 O。
- 两个红块初始只有角接触；上方红块由绿块支撑。
- 参考操作：移动至 x=0，直接落下；落在 `(0, 14)`，分别与两个红块通过边连接并消除。
- 目标：累计消除至少 3 个方块；绿块留在棋盘上也算成功。
- 提示：不是排成直线，而是通过边连成一组。

### 03 · 一座桥

- 初始：红 O `(0, 16)`，红 O `(6, 16)`。
- 待落序列：红 I（默认水平朝向）。
- 参考操作：移动至 x=2，直接落下；I 落在 y=17，连接左右两个 O。
- 目标：清空所有初始红块与本关待落红块。
- 提示：长条本身只有一个方块；它可以把两个分离的组连起来。

## 草案验证记录

已直接调用当前 C# `Board` / `Piece` 检查前三关：初始布局合法、稳定且不存在匹配；参考硬降落点分别为 `(4, 16)`、`(0, 14)`、`(2, 17)`；均消除三个完整方块，剩余方块数分别为 0、1、0。尚未接入有限序列、胜负目标与真实输入回放，也未穷举替代解法。

## 建议的关卡规则

- 使用固定初始棋盘与有限待落序列；相同关卡每次重试一致，不能在序列结束后补入随机块。
- 首章不自动下落，玩家移动、旋转或主动落下后才推进；消除与重力动画仍按现有节奏播放。后续再讨论是否引入计时挑战。
- 成功与失败均在当前消除、重力和连锁完全结束后判断。最后一个方块触发连锁时，不能因序列已空而提前判负。
- 待落序列耗尽仍未达标，或出生位置被阻挡且目标未完成，则失败；立即提供重试。
- 每关一个主要目标，不在首章同时叠加时间、分数、特定颜色、步数和清空率。
- 第一版只记录通关，不加三星评级、体力、付费提示或分支地图。
- 提示分两级：先解释需要观察的关系，再展示参考落点；默认不直接展示完整解答。
- 中文设计文档保持唯一说明入口；关卡名称、目标、提示与过关 / 失败文案均进入现有 en / zh-CN / ja 翻译体系。

## 实现前的验收要求

1. 初始棋盘无重叠、越界、意外三消或无支撑悬空块。
2. 每关至少一条通过真实移动、旋转、硬降操作可达到的解；只检查最终棋盘合法性不够。
3. 检查其他解法是否绕过本关教学点；允许合理多解，不靠隐藏条件强迫参考解。
4. 最后一步成功、连锁后成功、序列耗尽失败、出生阻挡失败、暂停与重试都有明确行为。
5. Godot 与网页使用同一关卡数据和目标判断，布局继续保持单屏可见。

## 待讨论

- 以手工解谜、随机挑战还是混合战役作为主线？
- 首章采用无自动下落的思考节奏，是否符合期望？
- 是否先做前三关可玩样例，验证乐趣与教学效果，再完成六关？
