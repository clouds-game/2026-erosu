# 验证记录

日期：2026-09-22。当前验收对象为 Godot + C# 原生项目。

## 构建与规则

- 环境：Godot .NET 4.7.2、.NET SDK 8.0.303。
- `dotnet restore ChromaDrop.csproj --force`：通过。
- `dotnet build ChromaDrop.csproj --no-restore`：通过，0 警告、0 错误。
- `dotnet run --project Tests/ChromaDrop.Tests.csproj --no-restore`：18 / 18 通过。
- Godot 无界面导入：成功，无脚本或资源错误。
- Godot 无界面启动 120 帧：正常退出，无运行时错误。

测试覆盖整块计数、非直线连通、角接触排除、颜色隔离、满行不自动消除、部分支撑、堆叠重力、二次匹配、同时消除多个组、示例 300 分、连锁 900 分、暂停冻结、软降与锁定延迟、结算期间禁止输入、边界碰撞、出生阻塞、七袋分布，以及 20 组固定随机种子的对局不变量。

## 原生界面验收

已通过原生 Godot 窗口检查：

- 画面仅有棋盘、必要 HUD 和按钮，没有网页式标题或宣传装饰。
- F2 / Space 示例显示三块消除、300 分和一次连锁。
- 重开清零当前分数，最高分保留。
- 移动、旋转和直接落地可以完成新的摆放。
- 暂停显示暂停提示，恢复可继续。
- 堆至出生区后显示 GAME OVER，暂停按钮禁用。
- 棋盘、预览、操作说明和按钮在窗口内可见。
- 发现 Retina 初始窗口过小，已按屏幕缩放比例修正并限制在可用区域内；重启后已目视确认。

音效合成和播放代码已通过编译及引擎启动，但未对音色或音量进行听感验收。尚未进行其他操作系统、触屏设备或正式导出包验收。

## 早期网页实验

早期网页实验已归档，不再作为当前交付入口，其开发服务器已停止。浏览器验收状态不再阻塞原生项目。后续开发与验证均以 Godot 项目为准。
